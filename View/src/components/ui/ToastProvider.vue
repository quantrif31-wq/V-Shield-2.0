<template><div class="vs-toasts" role="region" aria-label="Thông báo hệ thống"><div v-for="toast in toasts" :key="toast.id" class="vs-toast" :class="`is-${toast.type}`" :role="toast.type==='error'?'alert':'status'"><div><strong>{{toast.title}}</strong><p v-if="toast.message">{{toast.message}}</p></div><BaseButton variant="ghost" size="small" icon-only aria-label="Đóng thông báo" @click="remove(toast.id)"><template #icon>×</template></BaseButton></div></div></template>
<script setup>import BaseButton from './BaseButton.vue';import { useToasts } from '../../composables/useToasts';const {toasts,remove}=useToasts()</script>
<style scoped>.vs-toasts{position:fixed;right:var(--space-5);bottom:var(--space-5);z-index:1200;display:grid;gap:var(--space-3);width:min(380px,calc(100vw - 32px));pointer-events:none}.vs-toast{display:flex;align-items:flex-start;justify-content:space-between;gap:var(--space-3);padding:var(--space-4);border:1px solid var(--border-default);border-radius:var(--radius-card);background:var(--surface-raised);box-shadow:var(--shadow-overlay);pointer-events:auto}.vs-toast p{margin-top:var(--space-1);color:var(--text-secondary);font-size:var(--type-body-size)}.is-success{border-color:var(--status-success-border)}.is-warning{border-color:var(--status-warning-border)}.is-error{border-color:var(--status-danger-border)}@media(max-width:640px){.vs-toasts{right:var(--space-4);bottom:var(--space-4)}}</style>
